# Read the Docs build config
version: 2

build:
  # must be one of RTD's supported images, e.g. ubuntu-20.04 or ubuntu-22.04
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
